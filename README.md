# Fast-API

Modernized FastAPI project for serving a machine learning model that predicts if a banknote is genuine or fake.

## Quick start

```bash
cd Fast_Api
pip install -r requirements.txt
uvicorn Fast_Api.app:app --reload
```

See `Fast_Api/README.md` for endpoint details.
